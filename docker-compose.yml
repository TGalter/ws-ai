version: "3.8"

services:
  ws-ai:
    image: thalesgalter/ws-ai
    container_name: ws-ai
    volumes:
      - ./session-data:/app/session-data
    environment:
      - GEMINI_API_KEY=your_gemini_api_key
      - INSTRUCOES=Descreva como o bot deve se comportar
    restart: unless-stopped
